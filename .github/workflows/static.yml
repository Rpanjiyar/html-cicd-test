name: Static Site CI/CD with SonarCloud

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages-deployment"
  cancel-in-progress: false

jobs:
  symbol-check:
    runs-on: ubuntu-latest
    outputs:
      status: ${{ steps.check-symbols.outputs.status }}
      error: ${{ steps.check-symbols.outputs.error }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run symbol check
        id: check-symbols
        run: |
          set +e
          output=$(python3 symbol_check.py 2>&1)
          exit_code=$?

          if [ $exit_code -ne 0 ]; then
            echo "status=FAILED" >> $GITHUB_OUTPUT
            echo "error<<EOF" >> $GITHUB_OUTPUT
            echo "$output" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
            exit 1
          else
            echo "status=PASSED" >> $GITHUB_OUTPUT
            echo "error=" >> $GITHUB_OUTPUT
          fi

  manual-approval:
    needs: symbol-check
    if: needs.symbol-check.outputs.status == 'FAILED'
    runs-on: ubuntu-latest
    outputs:
      approved: ${{ steps.approval.outputs.approved }}
    steps:
      - name: Print error from symbol_check.py
        run: echo "${{ needs.symbol-check.outputs.error }}"

      - name: Wait for manual approval
        id: approval
        uses: trstringer/manual-approval@v1
        with:
          secret: ${{ secrets.GITHUB_TOKEN }}
          approvers: ${{ secrets.MANUAL_APPROVER }}
          issue-title: "Manual Approval Required"
          issue-body: |
            Symbol check failed in `symbol_check.py`.

            Error Output:
            ```
            ${{ needs.symbol-check.outputs.error }}
            ```

            Click "Approve" on this issue to proceed.

  sonarcloud:
    needs: [symbol-check, manual-approval]
    if: needs.symbol-check.outputs.status == 'PASSED' || needs.manual-approval.outputs.approved == 'true'
    runs-on: ubuntu-latest
    outputs:
      quality_gate: ${{ steps.quality-gate.outputs['quality-gate-status'] }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: SonarCloud Scan (HTML only)
        uses: SonarSource/sonarqube-scan-action@v5.0.0
        with:
          args: -Dsonar.inclusions=**/*.html
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      - name: Wait for Quality Gate result
        id: quality-gate
        uses: SonarSource/sonarqube-quality-gate-action@v1.1.0
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      - name: Debug Quality Gate output
        run: echo "Quality Gate Status = ${{ steps.quality-gate.outputs['quality-gate-status'] }}"

  deploy-and-notify:
    needs: sonarcloud
    runs-on: ubuntu-latest
    if: needs.sonarcloud.outputs.quality_gate == 'PASSED'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload static site
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'  # Root directory with your static HTML files

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Trigger Tester Repo Workflow
        run: |
          curl -X POST https://api.github.com/repos/Indra512/PlaywrightCucumberTypeScript/dispatches \
          -H "Accept: application/vnd.github+json" \
          -H "Authorization: Bearer ${{ secrets.TEST_REPO_PAT }}" \
          -d '{"event_type":"dev-completed"}'

      - name: Send email notification
        if: always()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USER }}
          password: ${{ secrets.EMAIL_PASS }}
          subject: Static Site Deployed! ${{ github.event.head_commit.message }}
          to: raushan.teknotrait@gmail.com
          from: CI Bot <${{ secrets.EMAIL_USER }}>
          body: |
            Hello,

            Your static HTML site has been successfully deployed to GitHub Pages!

            ✅ Commit: ${{ github.sha }}
            🕒 Time: ${{ github.event.head_commit.timestamp }}
            🌐 URL: ${{ steps.deployment.outputs.page_url }}

            Cheers,
            GitHub Actions
